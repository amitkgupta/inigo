---
image: cloudfoundry/inigo-ci

env:
  - GOROOT: /usr/local/go
  - PATH: $GOROOT/bin:$PATH
  - GOPATH: /tmp/build/src/inigo
  - GINKGO_PARALLEL: true
  - SHORT_TIMEOUT: 2s
  - LONG_TIMEOUT: 10s

script: |
  set -e

  cd $GOPATH
  ./src/github.com/cloudfoundry-incubator/inigo/scripts/dev-test-inner
